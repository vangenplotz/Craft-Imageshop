{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Imageshop plugin for Craft CMS
 *
 * ImageshopImage Field Settings
 *
 * @author    Vangen & Plotz AS
 * @copyright Copyright (c) 2018 Vangen & Plotz AS
 * @link      https://vangenplotz.no/
 * @package   Imageshop
 * @since     0.0.1
 */
#}

{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("vangenplotz\\imageshop\\assetbundles\\imageshop\\ImageshopAsset") %}

{% if token is null or name is null %}
  {% set settingsUrl = cpUrl('/settings/plugins/imageshop') %}
  <p class="warning">{{ 'Missing token or interface name, please add them in the <a href="#{ settingsUrl }">Imageshop plugin settings</a>.' }}</p>
{% endif %}

{{ forms.textField({
    label: 'Button text',
    instructions: 'Text to display on the button.',
    id: 'buttonText',
    name: 'buttonText',
    value: field['buttonText']})
}}

{{ forms.textField({
    label: 'Max number of images',
    instructions: 'Maximum number of images the user can select. 0 means infinite',
    id: 'maxSelect',
    name: 'maxSelect',
    value: field['maxSelect']})
}}